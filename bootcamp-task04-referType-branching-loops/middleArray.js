let firstArray = [1, 10];
let secArray = [2, 3, 4, 5];
let lastItem = firstArray.pop()
let newArray = firstArray.concat(secArray).concat(lastItem)
console.log("New array is below:)")
console.log(newArray)
