let browser;
if (browser === "Edge") {
  console.log("You hav got the Edge!");
} else if (
  browser === "Chrome" ||
  browser === "Firefox" ||
  browser === "Safari"
) {
  console.log("Okey we support these browsers too");
} else {
  console.log("We hope that this page looks ok!");
}
